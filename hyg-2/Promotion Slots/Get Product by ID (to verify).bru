meta {
  name: Get Product by ID (to verify)
  type: http
  seq: 10
}

get {
  url: http://localhost:3000/product/{{promotionId}}
  body: none
  auth: bearer
}

headers {
  ~Authorization: Bearer {{authToken}}
}

auth:bearer {
  token: {{authToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
