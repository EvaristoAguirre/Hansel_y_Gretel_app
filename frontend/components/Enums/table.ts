export enum TableModalType {
  CREATE = "create",
  EDIT = "edit",
}

export enum TableState {
  AVAILABLE = "available",
  OPEN = "open",
  PENDING_PAYMENT = "pending_payment",
  CLOSED = "closed",
}