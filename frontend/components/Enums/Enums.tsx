export enum TableState {
  AVAILABLE = "available",
  OPEN = "open",
  PENDING_PAYMENT = "pending_payment",
  CLOSED = "closed",
}


export enum OrderState {
  CANCELLED = 'cancelled',
  OPEN = 'open',
  PENDING_PAYMENT = 'pending_payment',
  CLOSED = 'closed',
}