export enum TableState {
  AVAILABLE = "available",
  OPEN = "open",
  PENDING_PAYMENT = "pending_payment",
  CLOSED = "closed",
}


export enum OrderState {
  PENDING = 'pending',
  CLOSED = 'closed',
}